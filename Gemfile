source "https://rubygems.org"

gem "jekyll", "~> 4.3.0"
gem "github-pages", "~> 227", group: :jekyll_plugins
# webrick isn't installed by default in Ruby 3, so we need to
# add it manually for now.
gem "webrick", "~> 1.7"
